{
  "rules": {
    "users": {
      ".read": "auth !== null",
      ".indexOn": ["email"],
      "$uid": {
        ".write": "$uid === auth.uid"
      }
    },
    "userBudgets": {
      "$uid": {
        ".read": "$uid === auth.uid",
        "$budgetId": {
          ".write": "$uid === auth.uid || root.child('budgets').child($budgetId).child('ownerId').val() === auth.uid || root.child('budgets').child($budgetId).child('sharedWith').child(auth.uid).exists()"
        }
      }
    },
    "budgets": {
      "$budgetId": {
        ".read": "data.child('ownerId').val() === auth.uid || data.child('sharedWith').child(auth.uid).exists()",
        ".write": "!data.exists() || data.child('ownerId').val() === auth.uid",
        "sharedWith": {
          "$sharedUid": {
            ".write": "auth.uid === root.child('budgets').child($budgetId).child('ownerId').val() || root.child('budgets').child($budgetId).child('sharedWith').child(auth.uid).exists()"
          }
        }
      }
    },
    "transactions": {
      "$budgetId": {
        ".read": "root.child('budgets').child($budgetId).child('ownerId').val() === auth.uid || root.child('budgets').child($budgetId).child('sharedWith').child(auth.uid).exists()",
        ".write": "root.child('budgets').child($budgetId).child('ownerId').val() === auth.uid || root.child('budgets').child($budgetId).child('sharedWith').child(auth.uid).exists()",
        "$transactionId": {
          ".validate": "newData.hasChild('userId') && newData.child('userId').val() === auth.uid"
        }
      }
    },
    "userActivity": {
      "$uid": {
        ".read": "$uid === auth.uid",
        ".write": "$uid === auth.uid"
      }
    }
  }
}
